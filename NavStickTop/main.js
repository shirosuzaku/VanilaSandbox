const nav = document.querySelector('.nav')

let navY

let x = false
let y = x

window.addEventListener('scroll',()=>{

    // navY = nav.getClientRects()[0].y

    // if(navY < 0 ){
    //     x = true
    // }else{
    //     x = false
    // }

    // if(x != y){
    //     // navY < 0 ? console.log("d") : console.log("u")
    //     navY < 0 
    //     ? nav.classList.add('sticky')
    //     : nav.classList.remove('sticky')
    // }

    // y = x

})